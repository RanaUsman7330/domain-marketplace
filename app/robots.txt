import { NextResponse } from 'next/server';

export async function GET() {
  const robots = `User-agent: *
Disallow: /admin/
Allow: /

Sitemap: ${process.env.NEXT_PUBLIC_BASE_URL || 'http://localhost:3000'}/sitemap.xml`;
  return new NextResponse(robots, { headers: { 'Content-Type': 'text/plain' } });
}

export async function POST(request: Request) {
  const content = await request.text();
  console.log('Updated robots.txt:', content);
  return NextResponse.json({ message: 'Robots.txt updated' });
}